using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CurveUpdater
{
    public IEnumerator UpdaterCoroutine(List<ConnectionCurve> curves, ConnectionCurve tempCurve)
    {
        while (true)
        {
            foreach (ConnectionCurve curve in curves)
            {

            }
            yield return null;
        }
    }
}
